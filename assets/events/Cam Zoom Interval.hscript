function onTrigger(data, time)
{
    var beat = Std.parseInt(data.value1);
    if (Math.isNaN(beat)) beat = -1;
    
	var intensity = Std.parseFloat(data.value2);
    if (Math.isNaN(intensity)) intensity = 1;

    game.zoomEveryBeat = beat;
    game.camZoomingMult = intensity;
}